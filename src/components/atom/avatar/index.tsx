import { tv } from "tailwind-variants";

export const Avatar = tv({
  variants: {},
  base: "w-10 h-10 object-fit rounded-full hover:outline outline-offset-2	outline-2 hover:outline-2 hover:opacity-80 transition outline-primary",
});
